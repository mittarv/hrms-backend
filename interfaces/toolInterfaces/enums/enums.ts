// Define the enum for key-value
export enum KeyValueActionEnum {
  EDIT = "edit",
  DELETE = "delete",
  CREATE = "create",
}

// Define the enum for notification type
export enum notificationTypeEnum {
  EMAIL = "email",
  PUSH = "push",
}

// Define the enum for approval status
export enum approvalStatusEnum {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
}

// Define the enum for feature type
export enum featureTypeEnum {
  FREE = "Free",
  PREMIUM = "Premium",
  FREEMIUM = "Freemium",
}

// Define the enum for plan type
export enum planTypeEnum {
  MONTHLY = "Monthly",
  YEARLY = "Yearly",
}

// Define the enum for approval status in caps
export enum approvalStatusCapsEnum {
  PENDING = "PENDING",
  APPROVED = "APPROVED",
  REJECTED = "REJECTED",
}
